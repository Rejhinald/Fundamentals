# Route to create a movie via POST request
POST    /movies                                  MoviesController.CreateMovie

# Route to movie list
GET     /movies                                  MoviesController.GetMovies

# Route to get movie
GET     /movies/:id                              MoviesController.GetMovie

# Route to update movie
PUT     /movies/:id                              MoviesController.UpdateMovie

# Route to delete movie
DELETE     /movies/:id                           MoviesController.DeleteMovie

# Basic GET route for home or other actions
GET     /                                       App.Index

# Route to serve static files
GET     /public/*filepath                        Static.Serve("public")

